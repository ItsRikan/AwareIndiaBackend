{
    "installCommand": "pip install -r requirements.txt",
    "devCommand": "uvicorn src.api.index:app --host 0.0.0.0 --port $PORT",
    "outputDirectory": "src/api",
    "env": {
        "PYTHON_VERSION": "3.13"
    },
    "functions": {
        "src/api/index.py": {
            "maxDuration": 60,
            "runtime": "python@3.13.1"
        }
    },
    "rewrites": [
        {
            "source": "/(.*)",
            "destination": "/src/api/index.py"
        }
    ]
}